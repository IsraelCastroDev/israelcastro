export type MenuItems = {
  name: string;
  href: string;
};

export type Proyecto = {
  name: string;
  description: string;
  img: string;
  url: string;
  technologies?: string[];
};

export type Habilidades = {
  name: string;
  img: string;
};
